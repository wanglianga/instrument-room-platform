version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    environment:
      - VITE_API_BASE_URL=/api
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
